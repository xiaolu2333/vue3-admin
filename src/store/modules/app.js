/* jshint esversion: 6 */

// 本模块的 state 状态数据：
const state = {
  count: 100,
};
const getters = {};
const mutations = {};
const actions = {};

export default {
  namespaced: true, // 开启命名空间，防止命名冲突
  state,
  getters,
  mutations,
  actions,
};
